# Button

import { Tab, Tabs } from "nextra-theme-docs";
import { Button, Card } from "@noirtribe/ui";
import { DocsComponentLayout } from "../../../../../packages/@noirtribe-ui/src/components/doc/DocsComponentLayout/index";

```tsx
type ButtonProps = ComponentProps<typeof Button>;
```

<div className="pb-24">
  <DocsComponentLayout
    propInfo={[
      {
        name: "className",
        type: "string",
        defaultValue: "undefined",
        description: "The CSS class name for styling the button.",
      },
      {
        name: "variant",
        type: "string",
        defaultValue: "default",
        description:
          "The variant style of the button. Possible values are: 'link', 'default', 'primary', 'secondary', 'tertiary', 'destructive', 'info', 'outline', 'subtle', 'ghost', null, or undefined.",
      },
      {
        name: "size",
        type: "string",
        defaultValue: "default",
        description:
          "The size of the button. Possible values are: 'default', 'sm', 'md', 'lg', 'xl', null, or undefined.",
      },
      {
        name: "onClick",
        type: "Function",
        defaultValue: "null",
        description:
          "A callback function to be executed when the button is clicked.",
      },
    ]}
  >
    <Button variant="default" size="xl" onClick={() => alert("Button clicked")}>
      Button
    </Button>
  </DocsComponentLayout>
</div>
