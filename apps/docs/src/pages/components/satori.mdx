# Satori

Satori (悟り) is a Japanese Buddhist term for awakening, "comprehension; understanding".

import { Tab, Tabs } from "nextra-theme-docs";
import { Button } from "ui";

`const propKeys = Object.keys(Button.propTypes);`

<div>
  <div>Box for component goes here</div>
  <Tabs items={["Props", "Code"]}>
    <Tab>
      **Props**: This will be a table to showcase what props are available & are
      required.
    </Tab>
    <Tab>
      **Code**: This is where the code goes for transparency. In order to fully
      understand under the hood for efficient implementation.
    </Tab>
  </Tabs>
  <Button variant="default" size="xl" onClick={() => alert("Button Clicked")}>
    Test
  </Button>
</div>
