# Progress

import { Progress, DocsComponentLayout } from "@noirtribe/ui";

```tsx
type ProgressProps = ComponentProps<typeof Progress>;
```

<div className="pb-24">
  <DocsComponentLayout
    propInfo={[
      {
        name: "className",
        type: "string",
        defaultValue: "undefined",
        description: "The CSS class name for styling the button.",
      },
      {
        name: "value",
        type: "number",
        defaultValue: "0",
        description:
          "The current value of the progress bar. Should be between 0 and 100.",
      },
    ]}
  >
    <Progress value={33} className="w-[60%]" />
  </DocsComponentLayout>
</div>
