# Slider

import { Slider, DocsComponentLayout } from "@noirtribe/ui";

```tsx
type SliderProps = ComponentProps<typeof Slider>;
```

<div className="pb-24">
  <DocsComponentLayout
    propInfo={[
      {
        name: "className",
        type: "string",
        defaultValue: "undefined",
        description: "The CSS class name for styling the button.",
      },
    ]}
  >
    <Slider
      defaultValue={[50]}
      max={100}
      step={1}
      className="w-[60%] bg-secondary"
    />
  </DocsComponentLayout>
</div>
